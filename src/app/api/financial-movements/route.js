import {NextResponse} from "next/server";
import {createClient} from "@supabase/supabase-js";
const url=process.env.NEXT_PUBLIC_SUPABASE_URL,anonKey=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY,serviceKey=process.env.SUPABASE_SERVICE_ROLE_KEY;
const fail=(message,status=400)=>NextResponse.json({error:message},{status});
async function requireAdmin(request){if(!url||!anonKey||!serviceKey)throw new Error("Configure as credenciais do Supabase.");const token=(request.headers.get("authorization")||"").replace(/^Bearer\s+/i,"");if(!token)return{error:fail("Sessão não enviada.",401)};const auth=createClient(url,anonKey),{data,error}=await auth.auth.getUser(token);if(error||!data.user)return{error:fail("Sessão inválida.",401)};const admin=createClient(url,serviceKey,{auth:{persistSession:false,autoRefreshToken:false}}),profile=await admin.from("user_profiles").select("role").eq("id",data.user.id).single();if(profile.error||profile.data?.role!=="admin")return{error:fail("Apenas administradores podem gerenciar o financeiro.",403)};return{admin,userId:data.user.id}}
export async function GET(request){try{const auth=await requireAdmin(request);if(auth.error)return auth.error;const {searchParams}=new URL(request.url),start=searchParams.get("start"),end=searchParams.get("end"),type=searchParams.get("type");let query=auth.admin.from("financial_movements").select("*").order("movement_date",{ascending:false}).order("created_at",{ascending:false});if(start)query=query.gte("movement_date",start);if(end)query=query.lte("movement_date",end);if(type&&type!=="all")query=query.eq("movement_type",type);const{data,error}=await query;if(error)throw error;return NextResponse.json({movements:data||[]})}catch(error){return fail(error.message||"Não foi possível consultar as movimentações.",500)}}
export async function POST(request){try{const auth=await requireAdmin(request);if(auth.error)return auth.error;const body=await request.json(),type=body.movementType;const amount=Number(body.amount);if(!["entrada","despesa"].includes(type))return fail("Tipo de movimentação inválido.");if(!Number.isFinite(amount)||amount<=0)return fail("Informe um valor maior que zero.");const category=String(body.category||"").trim()||"Outros";if(category==="Vale para vendedor"&&!body.sellerId)return fail("Selecione o usuário que receberá o vale.");let linkedUser=null;if(body.sellerId){const userResult=await auth.admin.from("user_profiles").select("id,full_name,email").eq("id",body.sellerId).maybeSingle();if(userResult.error)throw userResult.error;if(!userResult.data)return fail("O usuário selecionado não foi encontrado.");linkedUser=userResult.data}const row={movement_type:type,category,description:String(body.description||"").trim(),amount,movement_date:body.movementDate||new Date().toISOString().slice(0,10),seller_id:body.sellerId||null,seller_name:linkedUser?(linkedUser.full_name||linkedUser.email):null,notes:String(body.notes||"").trim()||null,status:"ativo",created_by:auth.userId,updated_by:auth.userId};if(!row.description)return fail("Informe a descrição.");const{data,error}=await auth.admin.from("financial_movements").insert(row).select("*").single();if(error)throw error;return NextResponse.json({movement:data},{status:201})}catch(error){return fail(error.message||"Não foi possível salvar a movimentação.",500)}}
export async function DELETE(request){try{const auth=await requireAdmin(request);if(auth.error)return auth.error;const id=new URL(request.url).searchParams.get("id");if(!id)return fail("Movimentação não informada.");const{error}=await auth.admin.from("financial_movements").update({status:"cancelado",updated_by:auth.userId,updated_at:new Date().toISOString()}).eq("id",id);if(error)throw error;return NextResponse.json({ok:true})}catch(error){return fail(error.message||"Não foi possível cancelar a movimentação.",500)}}